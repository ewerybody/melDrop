# attr
def control(attr, at, name='', default=0):
	'''
	creates and connects visible attribute controls at nodes
	'''
	
	
def exists(plug):
	'''
	shortcut to the attributeQuery command
	'''
	
	if attributeQuery('t', node=objs[i], exists=1):